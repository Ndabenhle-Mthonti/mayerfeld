export function EngagementEstimator() {
  return (
    <section
      id="estimator"
      className="section section--muted"
      aria-labelledby="estimator-heading"
    >
      <div className="section__inner">
        <h2 id="estimator-heading">Engagement estimator</h2>
        <p className="section__intro">
          Hours, team allocation, and KPI deliverables will appear here once
          the calculator is added.
        </p>
        <div className="estimator-panel">
          <p>No estimate yet. Select a service above to get started in a later phase.</p>
        </div>
      </div>
    </section>
  )
}
